import {uid,type LyricBlock,type Role} from '../types.js';
const punctuation=/^[\p{P}\p{S}]+$/u;
export function segmentKorean(text:string):string[]{const raw=[...new Intl.Segmenter('ko',{granularity:'grapheme'}).segment(text)].map(x=>x.segment);const out:string[]=[];let prefix='';for(const c of raw){if(/^\s+$/u.test(c))continue;if(punctuation.test(c)){if(out.length)out[out.length-1]+=c;else prefix+=c}else{out.push(prefix+c);prefix=''}}return out}
export function parseLyrics(input:string,roles:Role[]):{blocks:LyricBlock[];roles:Role[]}{const next=[...roles];const blocks=input.split(/\r?\n/).map(x=>x.trim()).filter(Boolean).map(line=>{const m=line.match(/^\[([^\]]+)]\s*(.*)$/);const roleName=m?.[1]?.trim()||'전체';const text=(m?.[2]??line).trim();let role=next.find(x=>x.name===roleName);if(!role){role={id:uid(),name:roleName,color:['#60a5fa','#c084fc','#34d399','#fb923c'][next.length%4]};next.push(role)}return{id:uid(),text,roleId:role.id,segments:segmentKorean(text).map(t=>({id:uid(),text:t,start:0,end:0}))}});return{blocks,roles:next}}
export function flatten(blocks:LyricBlock[]){return blocks.flatMap((b,bi)=>b.segments.map((s,si)=>({block:b,segment:s,bi,si})))}
