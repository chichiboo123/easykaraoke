import type{KaraokeProject}from'../types.js';import{renderFrame}from'./renderer.js';
export type ExportStatus={percent:number;stage:string};
export function exportSupport(){const C=window as unknown as {MediaRecorder?:typeof MediaRecorder};const types=['video/mp4;codecs=avc1.42E01E,mp4a.40.2','video/mp4','video/webm;codecs=vp9,opus'];return{supported:!!C.MediaRecorder,type:types.find(t=>MediaRecorder.isTypeSupported(t))||''}}
export async function exportVideo(project:KaraokeProject,audio:HTMLAudioElement,bg:CanvasImageSource|undefined,onProgress:(s:ExportStatus)=>void,signal:AbortSignal){await document.fonts.ready;const support=exportSupport();if(!support.supported||!support.type)throw new Error('이 브라우저에서는 영상 인코더를 사용할 수 없어요. 최신 Chrome 또는 Edge를 사용해 주세요.');const canvas=document.createElement('canvas');canvas.width=1920;canvas.height=1080;const ctx=canvas.getContext('2d',{alpha:false})!;const stream=canvas.captureStream(30);const ac=new AudioContext();const source=ac.createMediaElementSource(audio);const dest=ac.createMediaStreamDestination();source.connect(dest);source.connect(ac.destination);dest.stream.getAudioTracks().forEach(t=>stream.addTrack(t));const chunks:Blob[]=[];const rec=new MediaRecorder(stream,{mimeType:support.type,videoBitsPerSecond:8_000_000,audioBitsPerSecond:192_000});rec.ondataavailable=e=>{if(e.data.size)chunks.push(e.data)};const stopped=new Promise<void>((ok,no)=>{rec.onstop=()=>ok();rec.onerror=()=>no(new Error('영상 인코딩 중 문제가 생겼어요.'))});audio.currentTime=0;audio.playbackRate=1;rec.start(1000);await audio.play();const start=performance.now();await new Promise<void>((ok,no)=>{const tick=()=>{if(signal.aborted){rec.stop();audio.pause();no(new DOMException('취소됨','AbortError'));return}const t=audio.currentTime;renderFrame(ctx,project,t,bg);onProgress({percent:Math.min(99,Math.round(t/project.media.duration*100)),stage:'영상과 오디오 합치는 중'});if(audio.ended){rec.stop();ok()}else requestAnimationFrame(tick)};tick()});await stopped;stream.getTracks().forEach(t=>t.stop());source.disconnect();await ac.close();onProgress({percent:100,stage:'영상 완성'});const actual=rec.mimeType.includes('mp4')?'video/mp4':'video/webm';return new Blob(chunks,{type:actual})}
