import type {KaraokeProject,LyricBlock} from '../types.js';import{blockAt,segmentProgress}from'./frame.js';
const palettes={classic:['#07152f','#172a52','#ffd43b'],stage:['#16122c','#522278','#e9b8ff'],dream:['#172554','#6d28d9','#93c5fd'],classroom:['#0891b2','#34d399','#fde047'],retro:['#3b176f','#ef476f','#ffd166'],minimal:['#111827','#1f2937','#fbbf24']};
function rounded(ctx:CanvasRenderingContext2D,x:number,y:number,w:number,h:number,r:number){ctx.beginPath();ctx.roundRect(x,y,w,h,r);ctx.fill()}
function fit(ctx:CanvasRenderingContext2D,text:string,max:number,start:number){let s=start;while(s>42&&ctx.measureText(text).width>max){s-=2;ctx.font=ctx.font.replace(/^\d+px/,`${s}px`)}return s}
function drawLyric(ctx:CanvasRenderingContext2D,b:LyricBlock,y:number,time:number,color:string,font:string){ctx.textAlign='center';ctx.textBaseline='middle';ctx.font=`96px "${font}", "Noto Sans KR", sans-serif`;fit(ctx,b.text,1650,96);ctx.lineWidth=12;ctx.strokeStyle='rgba(0,0,0,.8)';ctx.strokeText(b.text,960,y);ctx.fillStyle='#fff';ctx.fillText(b.text,960,y);let x=960-ctx.measureText(b.text).width/2;ctx.textAlign='left';for(const s of b.segments){const w=ctx.measureText(s.text).width,p=segmentProgress(s,time);if(p>0){ctx.save();ctx.beginPath();ctx.rect(x,y-75,w*p,150);ctx.clip();ctx.fillStyle=color;ctx.fillText(s.text,x,y);ctx.restore()}x+=w}}
export function renderFrame(ctx:CanvasRenderingContext2D,p:KaraokeProject,time:number,bg?:CanvasImageSource){const[c1,c2,point]=palettes[p.style.preset];ctx.save();ctx.clearRect(0,0,1920,1080);const g=ctx.createLinearGradient(0,0,1920,1080);g.addColorStop(0,c1);g.addColorStop(1,c2);ctx.fillStyle=g;ctx.fillRect(0,0,1920,1080);if(bg){ctx.filter=`brightness(${p.style.brightness}%) blur(${p.style.blur}px)`;ctx.drawImage(bg,-20,-20,1960,1120);ctx.filter='none';ctx.fillStyle=`rgba(0,0,0,${p.style.darken/100})`;ctx.fillRect(0,0,1920,1080)}ctx.font=`32px "${p.style.fontFamily}",sans-serif`;ctx.fillStyle='rgba(255,255,255,.8)';ctx.textAlign='left';ctx.fillText([p.meta.musical,p.meta.number].filter(Boolean).join(' · '),100,80);ctx.textAlign='right';ctx.fillText(p.meta.title,1820,80);const activeMarker=p.markers.find(m=>time>=m.start&&time<=m.end);const first=p.lyricBlocks[0]?.segments[0]?.start??0;if((activeMarker?.type==='intro'||time<first)&&time<Math.max(first,6)){ctx.textAlign='center';ctx.fillStyle='#fff';ctx.font=`52px "${p.style.fontFamily}",sans-serif`;if(p.meta.musical)ctx.fillText(p.meta.musical,960,350);ctx.font=`86px "${p.style.fontFamily}",sans-serif`;ctx.fillText([p.meta.number,p.meta.title].filter(Boolean).join('  '),960,480);ctx.font=`34px "${p.style.fontFamily}",sans-serif`;ctx.fillText([[p.meta.composer&&`작곡 ${p.meta.composer}`],[p.meta.lyricist&&`작사 ${p.meta.lyricist}`]].flat().filter(Boolean).join('   '),960,570)}else if(activeMarker?.type==='interlude'){ctx.textAlign='center';ctx.fillStyle='#fff';ctx.font=`78px "${p.style.fontFamily}",sans-serif`;ctx.fillText(activeMarker.label||'간주',960,510)}else{const{block,next}=blockAt(p,time);if(block){const role=p.roles.find(r=>r.id===block.roleId);const fill=p.style.colorMode==='role'?(role?.color||point):point;if(role){ctx.fillStyle=role.color;rounded(ctx,850,250,220,65,32);ctx.fillStyle='#111827';ctx.textAlign='center';ctx.font=`32px "${p.style.fontFamily}",sans-serif`;ctx.fillText(role.name,960,284)}drawLyric(ctx,block,500,time,fill,p.style.fontFamily);if(next){ctx.font=`66px "${p.style.fontFamily}",sans-serif`;ctx.textAlign='center';ctx.lineWidth=9;ctx.strokeStyle='rgba(0,0,0,.75)';ctx.strokeText(next.text,960,680);ctx.fillStyle='rgba(255,255,255,.82)';ctx.fillText(next.text,960,680)}}}if(activeMarker?.countdown){const remain=Math.ceil(activeMarker.end-time);if(remain>0&&remain<=activeMarker.countdown){ctx.font=`120px "${p.style.fontFamily}",sans-serif`;ctx.textAlign='center';ctx.fillStyle='#fff';ctx.fillText(String(remain),960,880)}}if(p.style.progress&&p.media.duration){ctx.fillStyle='rgba(255,255,255,.25)';ctx.fillRect(100,1015,1720,8);ctx.fillStyle=point;ctx.fillRect(100,1015,1720*Math.min(1,time/p.media.duration),8)}ctx.restore()}
